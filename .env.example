# Example environment variables for MCP Kali Server

# Docker socket path (default: /var/run/docker.sock)
DOCKER_SOCKET=/var/run/docker.sock

# Container name (default: mcp-kali)
CONTAINER_NAME=mcp-kali

# Image name (default: mcp-kali-server:latest)
IMAGE_NAME=mcp-kali-server:latest

# Default timeout for commands in milliseconds (default: 300000)
DEFAULT_TIMEOUT=300000
